// Stub Kafka - à implémenter avec kafka client
export async function publish(topic: string, payload: any) {
    console.log(`[Kafka] Publishing to ${topic}:`, payload);
    // Implémentation réelle avec kafka client
    return;
}