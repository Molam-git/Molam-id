# ============================================================================
# Variables d'environnement pour la PRODUCTION
# ============================================================================
# IMPORTANT: Ne committez JAMAIS ce fichier avec les vraies valeurs !
# Utilisez les interfaces de Vercel et Render pour configurer les variables.
# ============================================================================

# API URL (à configurer dans Vercel)
# Sera automatiquement remplacé par l'URL de votre backend Render
EXPO_PUBLIC_API_URL=https://molam-id-api.onrender.com

# Backend (à configurer dans Render)
NODE_ENV=production
PORT=3000

# Database (à configurer dans Render - PostgreSQL fourni automatiquement)
DB_HOST=<your-render-postgres-host>
DB_PORT=5432
DB_USER=<your-render-postgres-user>
DB_PASSWORD=<your-render-postgres-password>
DB_NAME=molam

# JWT Secrets (générés automatiquement par Render)
JWT_SECRET=<generated-by-render>
JWT_REFRESH_SECRET=<generated-by-render>
