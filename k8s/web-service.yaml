apiVersion: v1
kind: Service
metadata:
  name: molam-id-web-service
  namespace: molam-id
  labels:
    app: molam-id-web
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: molam-id-web
