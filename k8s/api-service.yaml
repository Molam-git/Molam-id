apiVersion: v1
kind: Service
metadata:
  name: molam-id-api-service
  namespace: molam-id
  labels:
    app: molam-id-api
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: molam-id-api
